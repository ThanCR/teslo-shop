#TesloShop

1. Clone repository
2. Clone .env.template file and rename as .env
3. Run `npm install`, this is for installing dependencies
4. Verify backend is actually running on port 3000
5. Run `npm run dev` to start development server